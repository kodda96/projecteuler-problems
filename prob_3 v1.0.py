import time
start_time=time.time()
import math
def prime(num):
    for i in range(2,int(math.sqrt(num)+1)):
               if num%i==0:
                   return False
    return True
                   
if prime:
    number=str(num)
    x=str.index(str[-1])
    if len(str)>=2:
        
